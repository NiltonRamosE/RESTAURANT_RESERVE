<div class="bg-white p-6 rounded-lg shadow-md">
    <div class="mb-6">
        <img src="{{ $image }}" alt="{{ $name }}" class="w-32 h-32 mx-auto rounded-full">
    </div>
    <p class="text-gray-700 italic">“{{ $quote }}”</p>
    <p class="mt-4 text-sevensoup-green font-semibold">- {{ $name }}</p>
</div>
